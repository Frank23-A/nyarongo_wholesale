enum UserRole { customer, worker, admin }

enum OrderStatus { pending, confirmed, shipped, delivered, cancelled }

enum PaymentStatus { pending, paid, failed, refunded }

enum DeliveryStatus { scheduled, inTransit, delivered }
